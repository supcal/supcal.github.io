# 个人学术网站使用手册

**网站地址**：https://supcal.github.io
**后台地址**：https://supcal.github.io/admin
**代码仓库**：https://github.com/supcal/supcal.github.io

---

## 一、快速开始：登录后台

1. 打开 **https://supcal.github.io/admin**
2. 点击 **Login with GitHub** 按钮
3. 在 GitHub 授权页面点 **Authorize**（只需授权一次）
4. 进入后台后，左侧能看到六个内容集合：

| 集合 | 作用 |
|------|------|
| 个人简介 | 修改首页的介绍文字、研究方向、邮箱 |
| 新闻动态 | 发布/删除新闻（论文接收、获奖等） |
| 论文列表 | 增删改查论文（BibTeX 格式） |
| 科研项目 | 增删改查科研项目 |
| 教学课程 | 增删改查课程，**上传课件** |
| 奖励荣誉 | 增删改查奖励 |

> 所有修改点 **Publish**（发布）按钮后，GitHub Actions 会自动重新构建网站，约 1-3 分钟后线上生效。

---

## 二、各板块操作指南

### 2.1 修改个人简介
后台 → **个人简介** → 编辑姓名/单位/简介/研究方向 → 保存

### 2.2 发布新闻
后台 → **新闻动态** → 新建 → 填日期和内容（支持 Markdown 加粗等）→ 保存
示例内容：`论文被 **ICASSP 2026（CCF-B）** 接收 🎉`

### 2.3 添加论文
后台 → **论文列表** → 编辑 BibTeX → 按下面格式加一条 → 保存

```bibtex
@article{key2025new,
  title={论文标题},
  author={Li, Chao and 其他作者},
  journal={期刊名},
  year={2025},
  abstract={摘要（中文也可以）},
  selected={true},            # true=首页精选显示
  pdf={文件名.pdf}            # 有 PDF 就填（文件放 assets/pdf/ 或后台上传）
}
```

> 论文详情页（more → 进入）会自动生成，无需手动建页面。
> 引用次数可加 `citations={数字}` 字段显示。

### 2.4 上传课件（重要）
后台 → **教学课程** → 点击课程名（如《数据结构》）→ 在「教学周历与课件」→ 某周 → **课件材料** → 点 **+ 添加** → 材料名称 + **文件上传** → 保存

课件文件会上传到仓库 `assets/files/teaching/<课程>/`，学生点击课程页即可下载。

> 单文件建议 <50MB；大视频请放网盘，在 url 处粘贴网盘链接。

### 2.5 管理科研项目
后台 → **科研项目** → 新建/编辑 → 填写名称、简介、类别（主持/参与）、角色、时间、资助 → 保存

---

## 三、本地开发（可选）

如果你要在本地预览网站效果（需要电脑装有 Ruby）：

```bash
cd E:\Zcode\AI4research\personal-website
bundle install          # 首次安装依赖
bundle exec jekyll serve   # 本地预览 http://localhost:4000
```

> 本机未装 Ruby 时，直接改文件 → `git push` → 等 GitHub Actions 构建即可，线上验证。

---

## 四、常见问题

**Q1：后台改完保存后，网站没更新？**
A：GitHub Actions 构建需要 1-3 分钟，稍等刷新。也可以到仓库 Actions 页看构建状态。

**Q2：上传的课件文件在哪？**
A：在仓库 `assets/files/teaching/<课程名>/` 目录下，学生从课程详情页下载。

**Q3：想换个人照片？**
A：两种方式：① 后台首页没有照片表单，需改文件——把新照片命名为 `prof_pic.jpg` 放到仓库 `assets/img/` 目录（可用网页版 GitHub 直接上传）。

**Q4：新论文想显示在首页精选？**
A：在 BibTeX 条目里加 `selected={true}` 字段即可。

**Q5：后台登录提示错误？**
A：检查是否在 GitHub 上给该 OAuth App 授权；若长期未登录，重新授权一次。

---

## 五、技术架构备忘

| 组件 | 说明 |
|------|------|
| 框架 | Jekyll + al-folio 主题 |
| 托管 | GitHub Pages（gh-pages 分支） |
| 自动部署 | GitHub Actions `.github/workflows/deploy.yml` |
| 后台 | Decap CMS（`admin/` 目录） |
| 登录鉴权 | Vercel OAuth 代理（supcal-cms-oauth） |
| 内容存储 | `_data/*.yml`、`_bibliography/papers.bib`、`_news/`、`_projects/`、`_teachings/` |

---

*最后更新：2026-07-22*
